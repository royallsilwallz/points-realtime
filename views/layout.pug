doctype html
html
   head
      title My Map
      link(rel='stylesheet', href='https://unpkg.com/leaflet@1.5.1/dist/leaflet.css')
      link(href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css", rel="stylesheet")
      script(src='https://unpkg.com/leaflet@1.5.1/dist/leaflet.js')
   body
      //- h1 My Map
      script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js')

      script(src='/socket.io/socket.io.js')

      nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
         a.navbar-brand(href='#') My Map
         button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarNav', aria-controls='navbarNav', aria-expanded='false', aria-label='Toggle navigation')
            span.navbar-toggler-icon
         #navbarNav.collapse.navbar-collapse
            ul.navbar-nav
               li.nav-item.active
               a.nav-link(href='/')
                  | Home 
                  span.sr-only (current)
               li.nav-item
               a.nav-link(href='/add') Add Points
               li.nav-item
               a.nav-link(href='/data') Data
               li.nav-item
               a.nav-link(href='/clear') Clear All


      #mapid(style='width: 100%; height: 590px;')

      script.
         var mymap = L.map('mapid').setView([28.22455005047019, 83.99940490722656], 13);

         L.tileLayer('https://api.tiles.mapbox.com/v4/{id}/{z}/{x}/{y}.png?access_token=pk.eyJ1IjoibWFwYm94IiwiYSI6ImNpejY4NXVycTA2emYycXBndHRqcmZ3N3gifQ.rJcFIG214AriISLbB6B5aw', {
            maxZoom: 18,
            attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors, ' +
               '<a href="https://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, ' +
               'Imagery © <a href="https://www.mapbox.com/">Mapbox</a>',
            id: 'mapbox.streets'
         }).addTo(mymap);

      block content